[build]
target = "xtensa-esp32-none-elf"

[target.xtensa-esp32-none-elf]
runner = "espflash flash --monitor --port {{PORT}}"
rustflags = [
  "-C", "link-arg=-Tesp32.ld",
  "-C", "link-arg=--gc-sections",
]

[env]
ESP_IDF_TOOLS_INSTALL_DIR = "/opt/esp/idf"